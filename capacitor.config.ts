import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'org.quesers.www',
  appName: 'ios-guru',
  webDir: 'build',
  bundledWebRuntime: false
};

export default config;
